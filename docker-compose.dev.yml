# =============================================================================
# GastroPilot – Microservices Development Stack
# =============================================================================
# Lokale Entwicklungsumgebung mit allen 4 Services + nginx + postgres + redis
# Alle Services mit Hot-Reload für schnelle Entwicklung.
#
# Voraussetzung: .env-Datei anlegen (siehe .env.example)
#
# Starten:    docker compose -f docker-compose.dev.yml up -d
# Stoppen:    docker compose -f docker-compose.dev.yml down
# Logs:       docker compose -f docker-compose.dev.yml logs -f [service]
# Migration:  docker compose -f docker-compose.dev.yml exec core alembic upgrade head
#
# Services:
#   - nginx:         http://localhost:80   (Reverse Proxy)
#   - Frontend:      http://localhost:3001
#   - Core:          http://localhost:8000  (via nginx)
#   - Orders:        http://localhost:8001  (via nginx)
#   - AI:            http://localhost:8002  (via nginx)
#   - Notifications: http://localhost:8003  (via nginx)
#   - Database:      localhost:5432
#   - Redis:         localhost:6379
# =============================================================================

services:
  # ---------------------------------------------------------------------------
  # Infrastruktur
  # ---------------------------------------------------------------------------
  postgres:
    image: postgres:16-alpine
    container_name: gp-dev-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-gastropilot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gastropilot}
      POSTGRES_DB: ${POSTGRES_DB:-gastropilot}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql/init.sql:/docker-entrypoint-initdb.d/01_init.sql
      - ./sql/rls.sql:/docker-entrypoint-initdb.d/02_rls.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-gastropilot} -d ${POSTGRES_DB:-gastropilot}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  redis:
    image: redis:7-alpine
    container_name: gp-dev-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # Core Service (Auth, Users, Restaurants, Reservierungen, Menü)
  # ---------------------------------------------------------------------------
  core:
    build:
      context: ./gastropilot-backend
      dockerfile: services/core/Dockerfile
    container_name: gp-dev-core
    restart: unless-stopped
    env_file: ./gastropilot-backend/services/core/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-gastropilot}:${POSTGRES_PASSWORD:-gastropilot}@postgres:5432/${POSTGRES_DB:-gastropilot}
      DATABASE_ADMIN_URL: postgresql+asyncpg://${POSTGRES_ADMIN_USER:-gastropilot_admin}:${POSTGRES_ADMIN_PASSWORD:-gastropilot_admin_password}@postgres:5432/${POSTGRES_DB:-gastropilot}
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: development
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-gastropilot}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-gastropilot123}
      MINIO_BUCKET: gastropilot-uploads
      MINIO_PUBLIC_URL: http://localhost:9000/gastropilot-uploads
    volumes:
      - ./gastropilot-backend/services/core/app:/app/app
      - ./gastropilot-backend/packages:/packages
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # Orders Service (Bestellungen, Kitchen, WebSocket, SumUp)
  # ---------------------------------------------------------------------------
  orders:
    build:
      context: ./gastropilot-backend
      dockerfile: services/orders/Dockerfile
    container_name: gp-dev-orders
    restart: unless-stopped
    env_file: ./gastropilot-backend/services/orders/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-gastropilot}:${POSTGRES_PASSWORD:-gastropilot}@postgres:5432/${POSTGRES_DB:-gastropilot}
      DATABASE_ADMIN_URL: postgresql+asyncpg://${POSTGRES_ADMIN_USER:-gastropilot_admin}:${POSTGRES_ADMIN_PASSWORD:-gastropilot_admin_password}@postgres:5432/${POSTGRES_DB:-gastropilot}
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: development
    volumes:
      - ./gastropilot-backend/services/orders/app:/app/app
      - ./gastropilot-backend/packages:/packages
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/api/v1/orders/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # AI Service (Sitzplatz-Optimierung, Peak-Prognose, Empfehlungen)
  # ---------------------------------------------------------------------------
  ai:
    build:
      context: ./gastropilot-backend
      dockerfile: services/ai/Dockerfile
    container_name: gp-dev-ai
    restart: unless-stopped
    env_file: ./gastropilot-backend/services/ai/.env
    environment:
      REDIS_URL: redis://redis:6379/0
      ENVIRONMENT: development
    volumes:
      - ./gastropilot-backend/services/ai/app:/app/app
      - ./gastropilot-backend/packages:/packages
    command: uvicorn app.main:app --host 0.0.0.0 --port 8002 --reload
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8002/api/v1/ai/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # Notifications Service (E-Mail, SMS, WhatsApp via Celery)
  # ---------------------------------------------------------------------------
  notifications:
    build:
      context: ./gastropilot-backend
      dockerfile: services/notifications/Dockerfile
    container_name: gp-dev-notifications
    restart: unless-stopped
    env_file: ./gastropilot-backend/services/notifications/.env
    environment:
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      ENVIRONMENT: development
    volumes:
      - ./gastropilot-backend/services/notifications/app:/app/app
      - ./gastropilot-backend/packages:/packages
    command: uvicorn app.main:app --host 0.0.0.0 --port 8003 --reload
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8003/api/v1/notifications/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - internal

  # Celery Worker für Notifications
  notifications-worker:
    build:
      context: ./gastropilot-backend
      dockerfile: services/notifications/Dockerfile
    container_name: gp-dev-notifications-worker
    restart: unless-stopped
    env_file: ./gastropilot-backend/services/notifications/.env
    environment:
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
      ENVIRONMENT: development
    volumes:
      - ./gastropilot-backend/services/notifications/app:/app/app
      - ./gastropilot-backend/packages:/packages
    command: celery -A app.worker.celery_app worker --loglevel=info --concurrency=2
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # nginx Reverse Proxy
  # ---------------------------------------------------------------------------
  nginx:
    image: nginx:alpine
    container_name: gp-dev-nginx
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-80}:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    depends_on:
      - core
      - orders
      - ai
      - notifications
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # Frontend (Next.js mit Hot Reload) – Restaurant Dashboard
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./gastropilot-frontend
      dockerfile: Dockerfile
      target: deps
    container_name: gp-dev-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3001}:3001"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_BASE_URL: http://localhost:${HTTP_PORT:-80}
      SSR_API_BASE_URL: http://nginx:80
      NEXT_PUBLIC_API_PREFIX: api/v1
    volumes:
      - ./gastropilot-frontend/app:/app/app
      - ./gastropilot-frontend/components:/app/components
      - ./gastropilot-frontend/lib:/app/lib
      - ./gastropilot-frontend/hooks:/app/hooks
      - ./gastropilot-frontend/public:/app/public
      - ./gastropilot-frontend/tsconfig.json:/app/tsconfig.json
      - ./gastropilot-frontend/next.config.ts:/app/next.config.ts
      - ./gastropilot-frontend/postcss.config.mjs:/app/postcss.config.mjs
      - ./gastropilot-frontend/tailwind.config.ts:/app/tailwind.config.ts
      - /app/node_modules
      - /app/.next
    depends_on:
      - nginx
    command: sh -c "npm install && npm run dev"
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # Guest Portal (Next.js SSR) – Gast Web-Portal
  # ---------------------------------------------------------------------------
  guest-portal:
    build:
      context: ./gastropilot-guest-portal
      dockerfile: Dockerfile
      target: deps
    container_name: gp-dev-guest-portal
    restart: unless-stopped
    ports:
      - "${GUEST_PORTAL_PORT:-3002}:3002"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_BASE_URL: http://localhost:${HTTP_PORT:-80}
      SSR_API_BASE_URL: http://nginx:80
      NEXT_PUBLIC_API_PREFIX: api/v1
    volumes:
      - ./gastropilot-guest-portal/app:/app/app
      - ./gastropilot-guest-portal/components:/app/components
      - ./gastropilot-guest-portal/lib:/app/lib
      - ./gastropilot-guest-portal/public:/app/public
      - ./gastropilot-guest-portal/tsconfig.json:/app/tsconfig.json
      - ./gastropilot-guest-portal/next.config.ts:/app/next.config.ts
      - ./gastropilot-guest-portal/postcss.config.mjs:/app/postcss.config.mjs
      - ./gastropilot-guest-portal/i18n:/app/i18n
      - ./gastropilot-guest-portal/messages:/app/messages
      - ./gastropilot-guest-portal/middleware.ts:/app/middleware.ts
      - /app/node_modules
      - /app/.next
    depends_on:
      - nginx
    command: sh -c "npm install && npm run dev"
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # Table Order (Next.js PWA) – QR-Code-Bestellen am Tisch
  # ---------------------------------------------------------------------------
  table-order:
    build:
      context: ./gastropilot-table-order
      dockerfile: Dockerfile
      target: deps
    container_name: gp-dev-table-order
    restart: unless-stopped
    ports:
      - "${TABLE_ORDER_PORT:-3003}:3003"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_BASE_URL: http://localhost:${HTTP_PORT:-80}
      SSR_API_BASE_URL: http://nginx:80
      NEXT_PUBLIC_API_PREFIX: api/v1
    volumes:
      - ./gastropilot-table-order/app:/app/app
      - ./gastropilot-table-order/components:/app/components
      - ./gastropilot-table-order/lib:/app/lib
      - ./gastropilot-table-order/public:/app/public
      - ./gastropilot-table-order/tsconfig.json:/app/tsconfig.json
      - ./gastropilot-table-order/next.config.ts:/app/next.config.ts
      - ./gastropilot-table-order/postcss.config.mjs:/app/postcss.config.mjs
      - /app/node_modules
      - /app/.next
    depends_on:
      - nginx
    command: sh -c "npm install && npm run dev"
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # KDS Standalone (Next.js) – Kitchen Display System
  # ---------------------------------------------------------------------------
  kds:
    build:
      context: ./gastropilot-kds
      dockerfile: Dockerfile
      target: deps
    container_name: gp-dev-kds
    restart: unless-stopped
    ports:
      - "${KDS_PORT:-3004}:3004"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_BASE_URL: http://localhost:${HTTP_PORT:-80}
      SSR_API_BASE_URL: http://nginx:80
      NEXT_PUBLIC_API_PREFIX: api/v1
      NEXT_PUBLIC_WS_URL: ws://localhost:${HTTP_PORT:-80}/ws
    volumes:
      - ./gastropilot-kds/app:/app/app
      - ./gastropilot-kds/components:/app/components
      - ./gastropilot-kds/lib:/app/lib
      - ./gastropilot-kds/public:/app/public
      - ./gastropilot-kds/tsconfig.json:/app/tsconfig.json
      - ./gastropilot-kds/next.config.ts:/app/next.config.ts
      - ./gastropilot-kds/postcss.config.mjs:/app/postcss.config.mjs
      - /app/node_modules
      - /app/.next
    depends_on:
      - nginx
    command: sh -c "npm install && npm run dev"
    networks:
      - internal

  # ---------------------------------------------------------------------------
  # Minio (S3-kompatibler Object Storage für Bilder)
  # ---------------------------------------------------------------------------
  minio:
    image: minio/minio:latest
    container_name: gp-dev-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-gastropilot}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-gastropilot123}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - internal

networks:
  internal:
    driver: bridge
    name: gastropilot-dev

volumes:
  postgres_data:
    name: gastropilot-dev-postgres-data
  redis_data:
    name: gastropilot-dev-redis-data
  minio_data:
    name: gastropilot-dev-minio-data
